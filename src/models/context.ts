export interface hashTagContext {
  row: number;
  column: number;
  imageNumber: number;
  category: string;
}

export interface profileContext {
  row: number;
  column: number;
  imageNumber: number;
}
